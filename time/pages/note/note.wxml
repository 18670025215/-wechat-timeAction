<view>
  <swiper>
    <block wx:for="{{timeArr}}">
      <swiper-item data-id="{{item.id}}" data-title="{{item.title}}" bindtap="onNoteInfo" >{{item.title}} -- {{item.create_time}}</swiper-item>
    </block> 
  </swiper>
  <view></view>
</view>
